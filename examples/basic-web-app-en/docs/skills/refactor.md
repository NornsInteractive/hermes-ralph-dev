# Refactor-stage rules

- Do not change the external API contract
- Run authentication and task tests both before and after the refactor
- It is fine to extract shared validation, but do not introduce unnecessary frameworks
