# Review-stage rules

- Check whether token generation and token validation are consistent
- Check whether status values are validated against a whitelist
- Check whether error responses follow the documented API conventions
