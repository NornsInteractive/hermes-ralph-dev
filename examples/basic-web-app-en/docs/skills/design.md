# Design-stage rules

- Verify that the API design matches the schema before implementation
- Avoid introducing a full permission system too early
- New endpoints should be documented under `docs/api/` before implementation begins
